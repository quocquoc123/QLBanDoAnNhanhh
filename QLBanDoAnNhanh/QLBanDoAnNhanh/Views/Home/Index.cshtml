@* @model IEnumerable<QLBanDoAnNhanh.Models.SanPham>

@{
    ViewData["Title"] = "Trang Chủ";
}

<h1 style="font-size:25px; width:1200px; margin:auto;">Danh Sách Sản Phẩm</h1>

<div class="row">
    @foreach (var product in Model)
    {
        <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
                <img src="@Url.Content(product.HinhAnh1)" class="card-img-top" alt="Hình ảnh @product.TenSp" />
                <div class="card-body">
                    <h5 class="card-title">@product.TenSp</h5>
                    <p class="card-text">@product.GiaTien.ToString("C") VND</p>
                    <p class="card-text">Thành phần: @product.ThanhPhan</p>
                    <p class="card-text">Số lượng bán trong ngày: @product.SlbanTrongNgay</p>
                    <p class="card-text">Chi tiết: @product.ChitietSp</p>
                    <a href="@Url.Action("ChiTietSanPham", "SanPhams", new { id = product.MaSp })" class="btn btn-primary">Mua Ngay</a>
                </div>
            </div>
        </div>
    }
</div>

<style>
    .row {
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
        gap: 20px;
        margin-top: 20px;
    }

    .col-md-4 {
        flex: 1 0 300px; /* Minimum width 300px */
        max-width: 300px; /* Ensures no item is larger than 300px */
        display: flex;
        justify-content: center;
    }

    .card {
        border-radius: 10px;
        border: 1px solid #e0e0e0;
        overflow: hidden;
        text-align: center;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

        .card img {
            width: 100%;
            height: auto;
            object-fit: contain;
            padding: 10px;
            border-bottom: 1px solid #e0e0e0;
            margin-bottom: 10px; /* Thêm khoảng cách dưới ảnh */
        }

    .card-body {
        padding: 20px;
    }

    .card-title {
        font-size: 18px;
        font-weight: bold;
        margin-bottom: 10px;
    }

    .card-text {
        font-size: 16px;
        color: #555;
        margin-bottom: 15px;
        margin-top: 5px; /* Thêm khoảng cách trên cho văn bản */
    }

    .btn {
        background-color: #007bff;
        color: #fff;
        padding: 10px 20px;
        border-radius: 5px;
        font-size: 14px;
        text-transform: uppercase;
        text-decoration: none;
    }

        .btn:hover {
            background-color: #0056b3;
            color: #fff;
        }
</style> *@